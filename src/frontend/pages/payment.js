import React from 'react';
import {Layout} from './../elements/theme/components/layout';
import {PaymentContainer} from '../elements/payment/container/PaymentContainer';

export const PayPage = () => {
    return (
        <Layout>
            <PaymentContainer/>
        </Layout>
    );
};