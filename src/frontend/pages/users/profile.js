import React from 'react';
import {Layout} from './../../elements/theme/components/layout';
import {ProfileContainer} from '../../elements/profile/containers/profileContainer.js';

export const Profile = () => {
    return (
        <Layout>
            <ProfileContainer/>
        </Layout>
    );
};