import React from 'react';
import {Layout} from './../../elements/theme/components/layout';
import {LogInContainer} from '../../elements/login/containers/loginContainer.js';

export const LogIn = () => {
    return (
        <Layout>
            <LogInContainer/>
        </Layout>
    );
};